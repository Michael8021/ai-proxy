name = "ai-proxy"
main = "src/worker.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# 如果你有自定义域名，取消下面的注释并修改
# routes = [
#   { pattern = "your-domain.com/*", zone_name = "your-domain.com" }
# ]

# 开发配置
[dev]
port = 8787
